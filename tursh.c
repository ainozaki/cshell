#include <stddef.h>

#include "exec.h"

int main() {
  for (int i = 0; i < 1; i++) {
    /* input */

    /* build-in */

    /* exec */
    if (tursh_exec() != 0) {
      continue;
    }
  };

  return 0;
}
